---
title: "LeetCode 586: Customer Placing the Largest Number of Orders"
seoTitle: "LeetCode 586: Customer Placing the Largest Number of Orders"
seoDescription: "Find the customer with the largest number of orders using SQL techniques like GROUP BY and window functions for scalability"
datePublished: Tue Jan 27 2026 06:56:59 GMT+0000 (Coordinated Universal Time)
cuid: cmkw8u6f4000102kz92t48ina
slug: leetcode-586-customer-placing-the-largest-number-of-orders
tags: mysql, sql, leetcode, build-in-public, leetcode-solution, varchasvh, dequest

---

**Date:** January 27, 2026  
**Category:** SQL  
**Time Taken:** minute  
**Difficulty:** Easy

---

# Problem Statement

Write a solution to find the `customer_number` for the customer who has placed **the largest number of orders**.

The test cases are generated so that **exactly one customer** will have placed more orders than any other customer.

**Link:** [Customers Placing the Largest Number of Orders](https://leetcode.com/problems/customer-placing-the-largest-number-of-orders/description/?envType=problem-list-v2&envId=wehxh2aj)

---

# My Approach

## **Solution 1 (Simpler, faster on small tables):**

The simplest solution is to use GROUP BY on customer\_number and then ORDER BY the count of order\_number in descending order. This method is quick for smaller tables, but it doesn't scale well.

---

### Solution Code:

```sql
SELECT customer_number 
FROM Orders
GROUP BY customer_number
ORDER BY COUNT(order_number) DESC
LIMIT 1
```

## Solution 2 (Complex, More Scalable):

A more scalable and flexible alternative is to use Window Functions. We can use two CTEs to get the count of `order_number` and calculate the rank based on these counts in descending order. Finally, we select the `customer_number` where the rank is 1.

### Solution Code:

```sql
WITH customerTotals AS (
    SELECT 
        customer_number, 
        COUNT(order_number) AS order_total
    FROM Orders
    GROUP BY customer_number
),
rankedCustomers AS (
    SELECT 
        customer_number, 
        order_total, 
        DENSE_RANK() OVER(ORDER BY order_total DESC) AS rnk
    FROM customerTotals
)

SELECT customer_number
FROM rankedCustomers
WHERE rnk = 1
```

**Key Takeaway:** We can use any aggregate functions in the `ORDER BY` statement.

**Pattern**: Grouping | Window Functions

**Mistakes I Made:** In the first solution, I didn't know that we could use `COUNT()` in the `ORDER BY` statement.

**Series**: 90 Days of Data Engineering Progress: 21/90 problems completed

**Tags**: #DEQuest #LeetCode #SQL #DataEngineering #BuildInPublic